---
title: Web App Tester
description: Automated web app behaviour verification triggered from a GitHub PR or Issue using Playwright CLI (headless Chromium). Finds the testable URL, runs a structured test plan, and posts a step-by-step report as a GitHub comment.
---

The **Web App Tester** verifies web app behaviour for a GitHub PR or Issue by running browser-based tests via **Playwright CLI** (headless Chromium).

It is **PR/Issue-driven**: point it at a PR or issue number and it finds the testable preview URL from comments, uses the existing test plan (or generates one automatically), executes each step in a fresh headless browser session, and posts a structured test execution report directly on the PR or issue.

Single command: **`/test-web-app`**

---

## How It Works

```mermaid
flowchart TD
    A[/test-web-app pr N or issue N] --> B[Phase 1 — Gather test context]
    B --> B1[Fetch PR/Issue content, comments, commits, linked issues]
    B1 --> C{Test URL found in comments?}
    C -- No --> D[Post 'no URL found' comment and STOP]
    C -- Yes --> E{Test plan found?}
    E -- Yes --> F[Use existing test plan]
    E -- No --> G[Auto-generate test plan and post comment]
    F --> H[Phase 2 — Run Playwright session]
    G --> H
    H --> H1{Chromium already cached?}
    H1 -- Yes --> I[Skip install]
    H1 -- No --> J[Install Chromium via npx]
    I --> K[Open single headless session via playwright-cli]
    J --> K
    K --> L[Execute steps adaptively — snapshot, act, verify, retry up to 3x]
    L --> M[Close session, clean up temp files]
    M --> N[Phase 3 — Post test execution report]
    N --> O[Compose report from styles/report-template.md and post via gh]
```

The orchestrator runs three sequential phases, each backed by its own skill file:

1. **Phase 1 — Gather test context** (`skills/gather-test-context/SKILL.md`)
   - Fetches the full PR/issue body, all comments, commit messages, and linked issue content via the `gh` CLI.
   - Scans content for a testable URL with labels like `Preview URL:`, `Staging URL:`, `Deploy preview:`. Posts a comment and stops if none is found.
   - Applies the production-URL safety check (`PRODUCTION_WARNING`).
   - Uses an existing structured test plan from comments (including ones generated by `test-strategist`), or auto-generates one from the PR/issue context and posts it before execution.

2. **Phase 2 — Run Playwright session** (`skills/run-playwright-session/SKILL.md`)
   - Resolves the `playwright-cli` wrapper once and writes `_wat_pcli` (handles PATH, install, and Node-fallback cases).
   - Checks if Chromium is already cached; installs it only if missing (first run only, ~30–60s).
   - Opens a single headless session, takes a DOM snapshot before every interaction, and adapts each step to the live `eN` references.
   - Retries failed steps up to 3 times with 2-second waits; captures a screenshot on the final retry.
   - Closes the session and deletes `_wat_pcli` and any `_wat_screenshot_*.png` files.

3. **Phase 3 — Post test execution report** (`skills/post-test-report/SKILL.md`)
   - Computes the overall verdict (`PASSED` / `FAILED` / `BLOCKED`).
   - Composes a comment that strictly conforms to `styles/report-template.md` — no recommendations, no commentary, no root-cause analysis.
   - Posts it as a single GitHub comment via `providers/github.md`.

---

## Inputs

| Input | Source | Required | Description |
|---|---|---|---|
| `target_id` | Command argument | Yes | GitHub PR number (`pr 42`) or Issue number (`issue 88`) |
| Test URL | PR/issue comments | Yes | URL labelled `Preview URL:`, `Staging URL:`, `Deploy preview:`, etc. |
| Test plan | PR/issue comments | No | Structured numbered/bulleted steps; auto-generated if absent |

---

## Sample Prompts

**Test a specific PR:**

```text
/test-web-app pr 42
```

**Test a specific Issue:**

```text
/test-web-app issue 88
```

**Infer from current branch:**

```text
/test-web-app
```

---

## Step Statuses

| Status | Meaning |
|---|---|
| ✅ PASSED | Step executed, expected outcome observed |
| ❌ FAILED | Step executed, expected outcome NOT observed |
| 🔴 BLOCKED | Step could not execute after 3 retries, or skipped due to production URL |

Overall result is **PASSED** when all steps pass, **FAILED** when steps fail but all were attempted, and **BLOCKED** when one or more steps could not execute.

---

## Auto-Generated Test Plan

If no test plan is found in the PR/issue, the agent generates one covering:
- The primary user-facing change
- Implied UI interactions (forms, navigation, state changes, error states)
- At least one happy-path and one edge/negative scenario

The auto-generated plan is posted as a GitHub comment before execution begins.

---

## Safety Rules

- **No URL found** → posts a comment and stops immediately
- **Production URL** (no `staging`, `preview`, `dev`, `test`, `localhost`, or PR/issue number in domain) → read-only mode; form submissions and destructive actions are skipped and marked BLOCKED
- **Report is strictly bounded** — the posted comment contains only the defined results table and failure details; no suggested fixes, recommendations, or commentary are ever added
- **Credentials** — never included in any posted comment
- **Temp files** — always deleted after the run (`_wat_pcli` wrapper, `_wat_screenshot_*.png` capture files), even if execution fails

---

## Report Format

The plugin posts a single GitHub comment with this exact structure — nothing more:

```
🤖 web-app-tester — Test Execution Report
URL tested: https://preview.example.com
Total: 5 | ✅ Passed: 4 | ❌ Failed: 1 | 🔴 Blocked: 0
Overall: FAILED

| # | Step | Status |
|---|------|--------|
| 1 | Navigate to /login | ✅ PASSED |
| 2 | Fill email and password, click Sign In | ✅ PASSED |
| 3 | Verify redirect to /dashboard | ✅ PASSED |
| 4 | Click New Order button | ❌ FAILED |
| 5 | Verify order form appears | ✅ PASSED |

---

### Failed / Blocked Steps

**Step 4 — Click New Order button**
Reason: Button not found after 3 retries — element not in DOM
Screenshot: captured at point of failure
```

---

## Performance

| Phase | First run | Subsequent runs | Xianix executor (pre-installed) |
|---|---|---|---|
| `playwright-cli` install (if missing) | ~5–10s | 0s (already installed) | 0s always |
| Browser install | ~30–60s | 0s (cached) | 0s always |
| Test execution (9 steps, adaptive `playwright-cli` loop) | ~25–35s | ~25–35s | ~25–35s |
| **Total** | **~60–105s** | **~25–35s** | **~25–35s** |

Browser caching is automatic — the plugin checks for a cached Chromium before attempting any install.

For the **Xianix platform**: request that `playwright` and Chromium be pre-installed in `99xio/xianix-executor:latest` to eliminate the first-run install cost entirely.

---

## Prerequisites

- **Node.js 20+** — required by `playwright-cli` and the Chromium browser install
- **`gh` CLI** installed and authenticated (`gh auth login` or `GITHUB_TOKEN` env var)

See [docs/setup.md](docs/setup.md) for full setup instructions.

---

## Environment Variables

| Variable | Required | Purpose |
|---|---|---|
| `GITHUB_TOKEN` | Yes (if `gh auth login` not run) | Authenticate `gh` CLI for reading PRs/issues and posting comments |

---

## Quick Start

```bash
# Navigate to the GitHub repo you want to test
cd /path/to/your-repo

# Launch Claude Code with the plugin
claude --plugin-dir /path/to/xianix-plugins-official/plugins/web-app-tester

# Trigger a test run
/test-web-app pr 42
```

Make sure your PR has a comment with a testable URL, for example:
```
Preview URL: https://pr-42--my-app.netlify.app
```

---

## Xianix Platform Integration

The plugin is designed for the Xianix executor environment (Docker container, Python 3.12 + Node.js 20). It uses only:
- `gh` CLI (pre-installed in the executor)
- `node` / `npx` (pre-installed via Node.js 20)
- `Bash` — no MCP server required

No additional environment configuration is needed beyond `GITHUB_TOKEN`.

---

## What's in This Plugin

```
web-app-tester/
├── .claude-plugin/
│   ├── plugin.json                       # Manifest (no MCP server — CLI-based)
│   └── settings.json                     # Default agent: orchestrator
├── commands/
│   └── test-web-app.md                   # /test-web-app [pr <n> | issue <n>]
├── agents/
│   └── orchestrator.md                   # Slim coordinator — parses input and dispatches each phase skill
├── skills/
│   ├── test-web-app/SKILL.md             # Public-facing trigger skill
│   ├── gather-test-context/SKILL.md      # Phase 1: fetch PR/issue, find URL, find/generate test plan
│   ├── run-playwright-session/SKILL.md   # Phase 2: playwright-cli setup + adaptive step loop + cleanup
│   └── post-test-report/SKILL.md         # Phase 3: compose verdict + post via provider
├── providers/
│   └── github.md                         # gh CLI commands for fetching and posting
├── styles/
│   └── report-template.md                # Strict report structure with boundary rules
├── hooks/
│   ├── hooks.json
│   └── validate-prerequisites.sh         # Checks node and gh before any command
├── docs/
│   └── setup.md                          # Node.js + gh CLI setup guide
└── README.md
```

---

## License

MIT — same as the rest of this marketplace.
